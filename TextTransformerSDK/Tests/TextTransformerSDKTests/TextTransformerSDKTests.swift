import XCTest
@testable import TextTransformerSDK

final class TextTransformerSDKTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(TextTransformerSDK().text, "Hello, World!")
    }
}
